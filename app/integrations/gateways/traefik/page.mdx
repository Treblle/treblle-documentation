---
title: "Traefik Plugin for Treblle - Treblle Documentation"
metaTitle: "Traefik Plugin for Treblle - Treblle Documentation"
description: "Enhance API Intelligence with the Treblle plugin for Traefik. Get real-time insights, auto-generated documentation, and alerts. Follow the setup guide to integrate."
image: "/og-images/treblle-traefik.jpg"
---

import { NoteCard } from "~components/cards/note";
import PaginationCards from "~components/cards/pagination-cards";
import { TipCard } from "~components/cards/tip";
import { InfoTable } from "~components/tables/info-table";


## What you get with the plugin

This plugin allows you to integrate your [Traefik Proxy](https://traefik.io/traefik/) with [Treblle](https://treblle.com/) seamlessly so you can enjoy:

- Top-Notch Observability: Gain comprehensive insights into your API's operations.
- Auto-Generated API Documentation: Automatically keep your documentation up-to-date with your API changes.
- Alerting and Notifications: Stay informed with alerts on crucial API events and changes.

## Prerequisites

- Traefik: This plugin has been tested on v2 and v3
- [Treblle](https://app.treblle.com/) Account

## Installation

1. Obtain your API key and Project ID from [Treblle](https://app.treblle.com/)
2. Visit the Traefik [Plugin Catalog](https://plugins.traefik.io/plugins)
3. Search for `Treblle` and select Install.
4. Follow the displayed instructions to configure the plugin in your static and dynamic configurations.
5. Restart your Traefik instance
6. Enjoy!

## Configuration

<InfoTable title="Parameter" description="Type and Description">
    <InfoTable.Row
        title="ApiKey"
        description="string - Your API key obtained from Treblle (required)"
    />
    <InfoTable.Row
        title="ProjectId"
        description="string - Your Project ID from Treblle (required)"
    />
    <InfoTable.Row
        title="AdditionalFieldsToMask"
        description="array - Additional sensitive fields to mask"
    />
    <InfoTable.Row
        title="RoutesToBlock"
        description="array - Paths to routes that should be hidden"
    />
    <InfoTable.Row
        title="RoutesRegex"
        description="string - Regex to match and hide specific routes"
    />
</InfoTable>

## Example Usage

```yaml copy
# Static configuration
experimental:
  plugins:
    treblle:
      moduleName: "github.com/Treblle/TreblleTraefikPluginGo"
      version: "{version-from-github-releases}"

# Dynamic configuration
http:
  routers:
    my-router:
      entryPoints:
        - http
      middlewares:
        - my-plugin
      service: service-whoami
      rule: Host(`localhost`)

  services:
    service-whoami:
      loadBalancer:
        servers:
          - url: "http://localhost:8081"
        passHostHeader: true

  middlewares:
    my-plugin:
      plugin:
        treblle:
          ApiKey: "your-api-key"
          ProjectId: "your-project-id"
          AdditionalFieldsToMask:
            - "accessToken"
            - "refreshToken"
          RoutesToBlock:
            - "/api/user/login"
            - "/ping"
          RoutesRegex: "^/api/projects"
```

<NoteCard>
Make sure to replace `your-api-key` and `your-project-id` with your actual Treblle credentials.
</NoteCard>

<TipCard>
Use the `AdditionalFieldsToMask` parameter to ensure sensitive data like tokens and passwords are properly masked in your API logs.
</TipCard>

<PaginationCards />